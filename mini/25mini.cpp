#include <iostream>
#define I std::cin>>

int main() {
	int a,l,g,o,r,i,t,h,m[50000];
	for (I a; a--; std::puts(i?"NO":"YES")) {
		I l;
		o=r=0;
		for (g=2*l; g--; h=m[g]-t-r,i=g<l?o?t&&h||h>0?1:i:r+=h>0?h:0,t&&(o=1)&&h<0?1:i:0) I (g<l?t:m[g-l]);
	}
} // TODO: fix me
