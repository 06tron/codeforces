#include <iostream>
#include <algorithm>
using namespace std;

int main() {
	long long s,o,c,i,a,l[7<<14];
	for (cin>>s; s--; puts(i+l[c-1]-l[0]>o?"NO":"YES")) {
		cin>>c>>o;
		for (a=i=c; a--; i+=l[a]) cin>>l[a];
		sort(l, l+c);
	}
}
